import { getCenarioDemonstracao } from "@/lib/registros/queries"
import { formatarData, rotulo } from "@/lib/registros/demonstracao"
import { Badge } from "@/components/ui/badge"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
export default async function Page(){const d=await getCenarioDemonstracao();if(!d)return null;return <div className="space-y-5"><div><h2 className="text-xl font-bold">Participação</h2><p className="text-sm text-muted-foreground">Validade social, assentimento observado e viabilidade da intervenção.</p></div>{d.validade_social.length===0?<p className="rounded-xl border border-dashed p-8 text-center text-sm text-muted-foreground">Sem registros.</p>:d.validade_social.map((v,i)=><Card key={i}><CardHeader><div className="flex flex-wrap justify-between gap-2"><CardTitle className="text-lg">Validade social</CardTitle><Badge>{formatarData(v.registrado_em)}</Badge></div></CardHeader><CardContent className="space-y-4"><div className="grid gap-3 sm:grid-cols-3"><Nota titulo="Aceitabilidade" valor={v.aceitabilidade}/><Nota titulo="Viabilidade" valor={v.viabilidade}/><Nota titulo="Benefício percebido" valor={v.beneficio_percebido}/></div><div className="rounded-xl bg-muted p-4"><p className="text-xs text-muted-foreground">Respondente e assentimento</p><p className="font-semibold">{rotulo(v.respondente_tipo)} · {rotulo(v.assentimento_observado)}</p></div><p className="text-sm">{v.relato}</p></CardContent></Card>)}</div>}
function Nota({titulo,valor}:{titulo:string;valor:number}){return <div className="rounded-xl border p-4"><p className="text-xs text-muted-foreground">{titulo}</p><p className="text-2xl font-bold">{valor}/5</p></div>}

