import { getCenarioDemonstracao } from "@/lib/registros/queries"
import { LockKeyhole, UserRound } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
export default async function Page(){const d=await getCenarioDemonstracao();if(!d)return null;return <div className="space-y-5"><div><h2 className="text-xl font-bold">Equipe</h2><p className="text-sm text-muted-foreground">Profissionais explicitamente vinculados ao paciente fictício.</p></div><Card><CardHeader><CardTitle className="text-lg">Profissionais vinculados</CardTitle></CardHeader><CardContent><div className="flex items-center gap-4 rounded-xl border p-4"><div className="rounded-full bg-primary/10 p-3"><UserRound className="size-5 text-primary"/></div><div className="flex-1"><p className="font-bold">{d.profissional.nome}</p><p className="text-sm text-muted-foreground">{d.profissional.email}</p></div><Badge>Profissional responsável</Badge></div></CardContent></Card><Card className="border-primary/20 bg-primary/5"><CardContent className="flex gap-3 p-5"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-primary"/><div><p className="font-bold">Como a privacidade funciona</p><p className="mt-1 text-sm text-muted-foreground">O vínculo autoriza acesso ao paciente, mas autoria e observações privadas continuam separadas por profissional. Este modo demonstrativo não permite gerar compartilhamentos, editar vínculos ou acessar dados reais.</p></div></CardContent></Card></div>}

