import{getAcessosResponsavel,getPlanosClinicosPaciente,getProfile}from"@/lib/registros/queries";import{CompartilhamentoResponsavel}from"@/components/registros/compartilhamento-responsavel"
export default async function CompartilhamentoPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const[acessos,planos,profile]=await Promise.all([getAcessosResponsavel(id),getPlanosClinicosPaciente(id),getProfile()]);const alvos=planos.flatMap(p=>p.objetivos.flatMap(o=>o.alvos)).map(a=>({id:a.id,nome:a.nome,proprio:a.profissional_id===profile?.id}));return <div className="space-y-5"><div><h2 className="text-xl font-bold">Compartilhamento</h2><p className="text-sm text-muted-foreground">Escolha explicitamente o período e os dados clínicos apresentados aos responsáveis.</p></div><CompartilhamentoResponsavel pacienteId={id} acessos={acessos} alvos={alvos}/></div>}
