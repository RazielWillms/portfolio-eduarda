import{getCapacitacoesPaciente,getPlanosClinicosPaciente,getProfile,getValidadeSocialPaciente}from"@/lib/registros/queries";import{ValidadeSocial}from"@/components/registros/validade-social";import{CapacitacaoAplicadores}from"@/components/registros/capacitacao-aplicadores"
export default async function ParticipacaoPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const[planos,profile,validade,capacitacoes]=await Promise.all([getPlanosClinicosPaciente(id),getProfile(),getValidadeSocialPaciente(id),getCapacitacoesPaciente(id)]);return <div className="space-y-8"><div><h2 className="text-xl font-bold">Participação</h2><p className="text-sm text-muted-foreground">Validade social, assentimento e capacitação de aplicadores.</p></div><ValidadeSocial pacienteId={id} profissionalAtualId={profile?.id??""} planos={planos} registros={validade}/><CapacitacaoAplicadores pacienteId={id} profissionalAtualId={profile?.id??""} planos={planos} registros={capacitacoes}/></div>}
