import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812370000_portal_series_clinicas.sql"),"utf8")
describe("portal de séries clínicas",()=>{it("suporta todas as medidas sem expor observações",()=>{for(const tipo of["frequencia","taxa","duracao","latencia","percentual_oportunidades","tentativas_discretas","intervalo_parcial","intervalo_total","amostragem_momentanea","escala_independencia","intensidade"])expect(sql).toContain(tipo);expect(sql).not.toContain("observacoes_privadas");expect(sql).not.toContain("observacoes_abc")});it("valida alvos e escopo no backend",()=>{expect(sql).toContain("public.usuario_vinculado");expect(sql).toContain("invalid_target");expect(sql).toContain("a.profissional_id=auth.uid()")});it("persiste controles explícitos",()=>{for(const campo of["periodo_meses","alvo_ids","exibir_criterios","exibir_fases","exibir_integridade","exibir_contextos"])expect(sql).toContain(campo)});it("revoga o token anterior do mesmo profissional",()=>{expect(sql).toContain("update public.acessos_responsavel set ativo=false")})})
