import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812400000_compartilhar_analise_tentativas.sql"),"utf8")
describe("compartilhamento de tentativas",()=>{it("é opcional e desligado por padrão",()=>{expect(sql).toContain("p_exibir_analise_tentativas boolean default false");expect(sql).toContain("exibir_analise_tentativas")});it("expõe somente agregados",()=>{for(const campo of["independentes","comAjuda","incorretas","semResposta","latenciaMedia","ajudas"])expect(sql).toContain(campo);expect(sql).not.toContain("observacao")});it("parte do resultado já autorizado do token",()=>expect(sql).toContain("public.obter_acompanhamento_responsavel(p_token)"))})
