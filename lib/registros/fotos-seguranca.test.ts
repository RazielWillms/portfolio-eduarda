import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812480000_fotos_perfil_paciente.sql"),"utf8")
describe("segurança das fotos",()=>{it("mantém bucket privado e tamanho limitado",()=>{expect(sql).toContain("false,2097152");expect(sql).toContain("image/jpeg")});it("restringe upload e remoção por dono ou vínculo",()=>{expect(sql).toContain("public.usuario_vinculado");expect(sql).toContain("=auth.uid()");expect(sql).toContain("for delete to authenticated")});it("valida o caminho também nas RPCs",()=>{expect(sql).toContain("invalid_photo_path");expect(sql).toContain("atualizar_minha_foto");expect(sql).toContain("atualizar_foto_paciente")})})
