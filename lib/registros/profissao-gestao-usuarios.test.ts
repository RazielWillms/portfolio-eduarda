import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const ler=(arquivo:string)=>readFileSync(join(process.cwd(),arquivo),"utf8");describe("profissão na gestão de usuários",()=>{it("valida e atualiza o catálogo pelo backend",()=>{const sql=ler("supabase/migrations/20260812750000_profissao_na_gestao_usuarios.sql");expect(sql).toContain("usuario_tem_permissao('usuarios.editar')");expect(sql).toContain("p_profissao_id");expect(sql).toContain("and ativo")});it("separa profissão do papel de acesso",()=>{const form=ler("components/registros/usuario-form.tsx"),tabela=ler("components/registros/usuarios-tabela.tsx");expect(form).toContain("Profissão (opcional)");expect(form).toContain("Papel no sistema");expect(tabela).toContain("updateUsuarioProfissao")})})
