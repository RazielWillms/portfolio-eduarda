import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path"
const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812700000_corrigir_exclusao_ocorrencia_frequencia.sql"),"utf8")
describe("exclusão de ocorrência de frequência",()=>{it("usa permissões granulares",()=>{expect(sql).toContain("frequencia.gerenciar");expect(sql).toContain("frequencia.registrar_propria");expect(sql).not.toContain("usuario_coordenacao")});it("distingue motivo, inexistência e repetição",()=>{expect(sql).toContain("invalid_reason");expect(sql).toContain("occurrence_not_found");expect(sql).toContain("occurrence_already_cancelled")});it("restaura o compromisso sem conflitar com outra falta ativa",()=>{expect(sql).toContain("agendamento_status_anterior");expect(sql).toContain("not exists(select 1 from public.ocorrencias_frequencia outra")});it("mantém auditoria",()=>{expect(sql).toContain("OCORRENCIA_FREQUENCIA_CANCELADA")})})
