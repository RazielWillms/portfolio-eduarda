import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812440000_correcao_administrativa_fase.sql"),"utf8")
describe("correção administrativa de fase",()=>{it("exige confirmação e justificativa robusta",()=>{expect(sql).toContain("p_confirmar_correcao");expect(sql).toContain("length(trim(coalesce(p_motivo,'')))<20")});it("classifica correção separadamente",()=>expect(sql).toContain("'correcao_administrativa'"));it("fecha o atalho legado para usuários",()=>expect(sql).toContain("revoke execute on function public.alterar_fase_alvo(uuid,text,text)from authenticated"));it("preserva transições clínicas internas",()=>expect(sql).toContain("'decisao_clinica'"))})
