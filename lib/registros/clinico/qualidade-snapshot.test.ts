import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812460000_qualidade_dados_snapshot_revisao.sql"),"utf8")
describe("snapshot de qualidade",()=>{it("calcula indicadores no servidor",()=>{for(const campo of["cobertura_integridade_percentual","concordancia_media_percentual","competencia_aplicador_percentual"])expect(sql).toContain(campo)});it("isola dados pelo autor",()=>{expect(sql).toContain("s.profissional_id=auth.uid()");expect(sql).toContain("c.solicitante_id=auth.uid()");expect(sql).toContain("c.profissional_id=auth.uid()")})})
