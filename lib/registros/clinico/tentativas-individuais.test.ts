import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812390000_tentativas_individuais.sql"),"utf8")
describe("tentativas individuais",()=>{it("isola tentativas pelo autor da sessão",()=>{expect(sql).toContain("profissional_id=auth.uid()");expect(sql).toContain("s.profissional_id=auth.uid()");expect(sql).toContain("force row level security")});it("reconcilia total e independência com o agregado",()=>{expect(sql).toContain("v_total<>(v_registro.dados->>'oportunidades')::integer");expect(sql).toContain("v_independentes<>(v_registro.dados->>'respostas_independentes')::integer");expect(sql).toContain("trials_summary_mismatch")});it("limita resultados e níveis de ajuda",()=>{for(const v of["correta","incorreta","sem_resposta","independente","gestual","verbal","modelo","fisica_parcial","fisica_total"])expect(sql).toContain(v)});it("mantém detalhamento opcional",()=>{expect(sql).toContain("p_tentativas jsonb default'[]'")})})
