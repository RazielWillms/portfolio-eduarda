import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812410000_revisao_clinica_evidencias.sql"),"utf8")
describe("snapshot ampliado da revisão",()=>{it("calcula evidências no servidor",()=>{for(const campo of["criterio_vigente_id","ambientes","aplicadores","snapshot_versao"])expect(sql).toContain(campo);expect(sql).toContain("s.profissional_id=auth.uid()")});it("exige confirmação em decisões terminais",()=>{expect(sql).toContain("p_decisao in('avancar_fase','encerrar')");expect(sql).toContain("limitations_not_confirmed")});it("não aceita snapshot enviado pelo cliente",()=>expect(sql).not.toContain("p_snapshot"))})
