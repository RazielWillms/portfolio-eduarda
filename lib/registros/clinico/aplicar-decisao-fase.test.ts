import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812420000_aplicar_decisao_fase.sql"),"utf8")
describe("aplicação da decisão de fase",()=>{it("aceita apenas revisão do próprio autor",()=>{expect(sql).toContain("v_revisao.profissional_id<>auth.uid()");expect(sql).toContain("public.usuario_pode_editar_alvo")});it("limita a transição a uma fase adjacente",()=>{for(const trecho of["'rascunho'then'linha_de_base'","'linha_de_base'then'ensino'","'ensino'then'generalizacao'","'generalizacao'then'manutencao'"])expect(sql).toContain(trecho)});it("impede reaplicação e vincula o histórico",()=>{expect(sql).toContain("historico_fase_revisao_unique");expect(sql).toContain("revisao_clinica_id=v_revisao.id")})})
