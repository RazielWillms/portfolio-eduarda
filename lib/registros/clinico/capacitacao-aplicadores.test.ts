import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812240000_capacitacao_aplicadores.sql"),"utf8")
describe("capacitação de aplicadores",()=>{it("separa componentes do treino da competência observada",()=>{expect(sql).toContain("instrucao_realizada boolean");expect(sql).toContain("ensaio_realizado boolean");expect(sql).toContain("competencia_percentual numeric")});it("restringe alvo e acesso ao profissional",()=>{expect(sql).toContain("a.profissional_id=auth.uid()");expect(sql).toContain("profissional_id=auth.uid()")});it("não permite escrita direta nem audita observações",()=>{expect(sql).not.toMatch(/create policy .* for insert/);const a=sql.slice(sql.indexOf("create or replace function public.auditar_capacitacao_aplicador"));expect(a).not.toContain("new.observacoes");expect(a).not.toContain("new.habilidades_treinadas")})})
