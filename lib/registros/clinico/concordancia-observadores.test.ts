import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812250000_concordancia_interobservadores.sql"),"utf8")
describe("concordância entre observadores",()=>{it("mantém a referência original em tabela sem policy de leitura",()=>{expect(sql).toContain("create table if not exists public.concordancia_referencias");expect(sql).not.toMatch(/create policy .* on public\.concordancia_referencias/)});it("exige observador distinto, ativo e vinculado",()=>{expect(sql).toContain("check(solicitante_id<>observador_id)");expect(sql).toContain("public.paciente_psicologos");expect(sql).toContain("p.status='ativo'")});it("resposta só ocorre uma vez e o cálculo é feito no servidor",()=>{expect(sql).toContain("s.status<>'pendente'");expect(sql).toContain("least(abs(v_ref),abs(p_valor_observador))*100");expect(sql).toContain("for update")});it("não audita valores cegos ou independentes",()=>{const a=sql.slice(sql.indexOf("create or replace function public.auditar_concordancia"));expect(a).not.toContain("new.valor_observador");expect(a).not.toContain("valor_referencia")})})
