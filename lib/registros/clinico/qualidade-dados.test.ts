import{describe,expect,it}from"vitest";import{avaliarQualidadeDados}from"./qualidade-dados";import type{AlvoClinicoCompleto,CapacitacaoAplicador,SolicitacaoConcordancia}from"./modelo";import type{PontoSerieClinica}from"./serie-clinica"
const alvo={id:"a"}as AlvoClinicoCompleto,ponto=(data:string,integridade:number|null)=>({id:data,data,integridade,valor:1,tipo:"frequencia",unidade:"ocorrências",fase:"ensino",ambiente:"clinica",aplicador:"profissional",numerador:null,denominador:null,protocoloVersao:1})as PontoSerieClinica
describe("qualidade dos dados",()=>{it("explicita ausência de verificações",()=>{const r=avaliarQualidadeDados(alvo,[ponto("2026-08-10",null)],[],[],"p",new Date("2026-08-12T12:00:00"));expect(r.find(i=>i.id==="cobertura_integridade")?.estado).toBe("atencao");expect(r.find(i=>i.id==="concordancia")?.estado).toBe("sem_dados")});it("consolida cobertura, concordância e competência",()=>{const pontos=[ponto("2026-08-01",100),ponto("2026-08-10",80)],ioa=[{alvo_id:"a",solicitante_id:"p",status:"concluida",concordancia_percentual:90}]as SolicitacaoConcordancia[],cap=[{alvo_id:"a",profissional_id:"p",competencia_percentual:85,criterio_competencia:"80%",realizado_em:"2026-08-01"}]as CapacitacaoAplicador[];const r=avaliarQualidadeDados(alvo,pontos,ioa,cap,"p",new Date("2026-08-12T12:00:00"));expect(r.every(i=>i.estado==="adequado")).toBe(true)});it("não usa concordância nem capacitação de outro profissional",()=>{const ioa=[{alvo_id:"a",solicitante_id:"outro",status:"concluida",concordancia_percentual:100}]as SolicitacaoConcordancia[],cap=[{alvo_id:"a",profissional_id:"outro",competencia_percentual:100,realizado_em:"2026-08-01"}]as CapacitacaoAplicador[];const r=avaliarQualidadeDados(alvo,[],ioa,cap,"p");expect(r.find(i=>i.id==="concordancia")?.estado).toBe("sem_dados");expect(r.find(i=>i.id==="competencia")?.estado).toBe("sem_dados")})})
