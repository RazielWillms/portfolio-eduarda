import{describe,expect,it}from"vitest";import{avaliarProntidaoRevisao}from"./prontidao-revisao";import type{AlvoClinicoCompleto,RegistroValidadeSocial}from"./modelo"
describe("prontidão para revisão",()=>{const alvo={id:"a",profissional_id:"p1",medicoes:[],criterios:[],protocolos:[]}as unknown as AlvoClinicoCompleto;it("não recomenda mudança sem evidências",()=>{const itens=avaliarProntidaoRevisao(alvo,[]);expect(itens).toHaveLength(7);expect(itens.every(i=>i.estado!=="atendido")).toBe(true);expect(itens.find(i=>i.id==="validade_social")?.estado).toBe("sem_dados")});it("considera validade e assentimento atuais do próprio profissional",()=>{const registro={id:"v",paciente_id:"pac",alvo_id:"a",profissional_id:"p1",objetivo_relevante:true,aceitabilidade:4,viabilidade:5,beneficio_percebido:4,assentimento_observado:"aceite",registrado_em:"2026-08-01"}as RegistroValidadeSocial;const itens=avaliarProntidaoRevisao(alvo,[],[registro],"p1",new Date("2026-08-12T12:00:00"));expect(itens.find(i=>i.id==="validade_social")?.estado).toBe("atendido");expect(itens.find(i=>i.id==="assentimento")?.estado).toBe("atendido")});it("não usa registro de outro profissional",()=>{const registro={alvo_id:"a",profissional_id:"p2",registrado_em:"2026-08-01"}as RegistroValidadeSocial;const itens=avaliarProntidaoRevisao(alvo,[],[registro],"p1",new Date("2026-08-12T12:00:00"));expect(itens.find(i=>i.id==="validade_social")?.estado).toBe("sem_dados")})})
