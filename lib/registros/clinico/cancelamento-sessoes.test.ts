import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812380000_cancelamento_sessoes.sql"),"utf8")
describe("cancelamento de sessões",()=>{it("restringe cancelamento e restauração ao autor vinculado",()=>{expect(sql.match(/v_sessao\.profissional_id<>auth\.uid\(\)/g)?.length).toBe(2);expect(sql.match(/public\.usuario_vinculado\(v_sessao\.paciente_id\)/g)?.length).toBe(2)});it("exige justificativa e preserva soft delete",()=>{expect(sql).toContain("length(trim(coalesce(p_motivo,'')))<5");expect(sql).toContain("status='cancelada',deleted_at=now()");expect(sql).toContain("status='finalizada',deleted_at=null")});it("audita sem copiar a justificativa clínica",()=>{expect(sql).toContain("SESSAO_CANCELADA");expect(sql).toContain("SESSAO_RESTAURADA");expect(sql).toContain("'motivo_informado',true");expect(sql).not.toContain("'motivo',p_motivo")})})
