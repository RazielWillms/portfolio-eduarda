import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const ler=(arquivo:string)=>readFileSync(join(process.cwd(),arquivo),"utf8"),sql=ler("supabase/migrations/20260812770000_listagem_usuarios_paginada.sql");describe("listagem paginada de usuários",()=>{it("exige permissão e exclui demonstração",()=>{expect(sql).toContain("usuarios.visualizar");expect(sql).toContain("usuario_demonstracao")});it("filtra e pagina no banco",()=>{expect(sql).toContain("p_profissao_id");expect(sql).toContain("p_papel");expect(sql).toContain("p_status");expect(sql).toContain("limit least");expect(sql).toContain("offset greatest")});it("não retorna credenciais ou metadados de autenticação",()=>{for(const termo of["password","encrypted","last_sign_in","token"])expect(sql).not.toContain(termo)})})
