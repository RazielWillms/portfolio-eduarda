import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path"
const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812690000_gestao_papeis_permissoes.sql"),"utf8")
describe("gestão de papéis",()=>{it("restringe todas as operações ao principal",()=>{expect((sql.match(/usuario_admin_principal\(\)/g)??[]).length).toBeGreaterThanOrEqual(4)});it("não aceita permissões fora do catálogo",()=>{expect(sql).toContain("invalid_permission")});it("protege o principal e papéis do sistema",()=>{expect(sql).toContain("main_admin_is_protected");expect(sql).toContain("system_role_is_protected")});it("reduz o legado de papel personalizado ao menor privilégio",()=>{expect(sql).toContain("else'profissional'end")});it("audita por meio das tabelas protegidas",()=>{expect(sql).toContain("perfil_papel_acesso")})})
