import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";const ler=(a:string)=>readFileSync(join(process.cwd(),a),"utf8"),sql=ler("supabase/migrations/20260812740000_catalogo_profissoes_buscas_operacionais.sql");describe("catálogo de profissões e buscas operacionais",()=>{it("normaliza profissão sem misturar papel de acesso",()=>{expect(sql).toContain("create table if not exists public.profissoes");expect(sql).toContain("profiles add column if not exists profissao_id");expect(sql).toContain("atualizar_meus_dados_profissionais_v2")});it("protege a administração do catálogo",()=>{expect(sql).toContain("admin_principal");expect(sql).toContain("salvar_profissao")});it("pagina buscas e exclui demonstração",()=>{expect(sql).toContain("count(*)over()");expect(sql).toContain("limit least");expect(sql).toContain("paciente_demonstracao");expect(sql).toContain("usuario_demonstracao")});it("não expõe conteúdo clínico nas buscas",()=>{for(const termo of["diagnostico","observacoes_privadas","observacao_administrativa"])expect(sql).not.toContain(termo)});it("usa seletores modais nas telas operacionais",()=>{const seletor=ler("components/registros/seletor-busca-operacional.tsx"),agenda=ler("components/registros/novo-agendamento-form.tsx"),agendaLista=ler("components/registros/agenda-operacional.tsx"),frequencia=ler("components/registros/frequencia-painel.tsx");expect(seletor).toContain("DialogContent");expect(seletor).toContain("Todas as profissões");expect(agenda).toContain("SeletorBuscaOperacional");expect(agendaLista.match(/SeletorBuscaOperacional/g)?.length).toBeGreaterThanOrEqual(3);expect(frequencia).toContain("SeletorBuscaOperacional")})})
