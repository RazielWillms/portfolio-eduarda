import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260812470000_endurecimento_operacional.sql"),"utf8")
describe("endurecimento operacional",()=>{it("revoga contratos substituídos",()=>{for(const nome of["registrar_sessao_clinica_v5","criar_revisao_clinica_alvo_v3","obter_acompanhamento_responsavel(text)"])expect(sql).toContain(nome)});it("concede contratos atuais somente quando existem",()=>{for(const nome of["registrar_sessao_clinica_v6","criar_revisao_clinica_alvo_v4","criar_acesso_responsavel_v2","obter_acompanhamento_responsavel_v2"])expect(sql).toContain(`public.${nome}`);expect(sql).toContain("to_regprocedure(item.assinatura)")});it("remove privilégios implícitos para objetos futuros",()=>{expect(sql).toContain("revoke create on schema public");expect(sql).toContain("alter default privileges in schema public revoke execute on functions from public")})})
