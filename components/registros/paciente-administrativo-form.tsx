"use client"
import{useState,type FormEvent}from"react";import{useRouter}from"next/navigation";import{cadastrarPacienteAdministrativo}from"@/lib/registros/actions";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label"
export function PacienteAdministrativoForm(){const router=useRouter(),[erro,setErro]=useState(""),[salvando,setSalvando]=useState(false);async function enviar(e:FormEvent<HTMLFormElement>){e.preventDefault();setSalvando(true);setErro("");const f=new FormData(e.currentTarget),r=await cadastrarPacienteAdministrativo({nome:String(f.get("nome")),responsavel:String(f.get("responsavel")),cpfResponsavel:String(f.get("cpfResponsavel")??""),cpfPaciente:String(f.get("cpfPaciente")??""),nascimento:String(f.get("nascimento")),contatos:String(f.get("contatos")??"")});if("error"in r){setErro(r.error);setSalvando(false)}else router.push("/registros/agenda")}return <form onSubmit={enviar}className="w-full max-w-4xl space-y-5 rounded-2xl border bg-card p-5 shadow-sm sm:p-6"><div className="grid gap-5 sm:grid-cols-2"><Campo label="Nome completo"><Input name="nome"required/></Campo><Campo label="Data de nascimento"><Input name="nascimento"type="date"required/></Campo><Campo label="Nome do responsável"><Input name="responsavel"required/></Campo><Campo label="Contatos"><Input name="contatos"placeholder="Telefone ou e-mail"/></Campo><Campo label="CPF do paciente (opcional)"><Input name="cpfPaciente"inputMode="numeric"/></Campo><Campo label="CPF do responsável (opcional)"><Input name="cpfResponsavel"inputMode="numeric"/></Campo></div><p className="text-xs text-muted-foreground">Este cadastro não concede acesso clínico nem cria vínculo com profissionais. O paciente ficará disponível para organização da agenda.</p>{erro&&<p className="text-sm text-destructive">{erro}</p>}<div className="flex gap-2"><Button disabled={salvando}>{salvando?"Salvando...":"Cadastrar paciente"}</Button><Button type="button"variant="secondary"onClick={()=>router.back()}>Cancelar</Button></div></form>}
function Campo({label,children}:{label:string;children:React.ReactNode}){return <div className="space-y-2"><Label>{label}</Label>{children}</div>}


